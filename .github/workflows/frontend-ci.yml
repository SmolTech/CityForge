name: Frontend CI

on:
  push:
    branches: [main, develop]
    paths:
      - "src/**"
      - "public/**"
      - "tests/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
      - "next.config.ts"
      - "tailwind.config.ts"
      - "vitest.config.ts"
      - "eslint.config.mjs"
      - "prisma/**"
      - ".github/workflows/frontend-ci.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "src/**"
      - "public/**"
      - "tests/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
      - "next.config.ts"
      - "tailwind.config.ts"
      - "vitest.config.ts"
      - "eslint.config.mjs"
      - "prisma/**"
      - ".github/workflows/frontend-ci.yml"

jobs:
  quality-checks:
    name: Code Quality & Security
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: cityforge_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
            .next/cache
            node_modules/.cache
          key: ${{ runner.os }}-frontend-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-frontend-

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate
        env:
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/cityforge_test"

      - name: Setup test database
        run: npx prisma db push
        env:
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/cityforge_test"

      - name: TypeScript check
        run: npm run typecheck

      - name: ESLint check
        run: npm run lint

      - name: Prettier formatting check
        run: npm run format:check

      - name: Security Tests - Log Redaction
        run: npm run test:run -- src/lib/utils/log-redaction.test.ts
        env:
          NODE_ENV: test
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/cityforge_test"

      - name: Run all tests
        run: npm run test:run
        env:
          NODE_ENV: test
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/cityforge_test"

      - name: Generate coverage report
        run: npm run test:coverage
        env:
          NODE_ENV: test
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/cityforge_test"

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          flags: frontend
          name: frontend-coverage
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Coverage Summary
        run: |
          echo "📊 Code Coverage Summary:"
          cat coverage/coverage-summary.json | grep -E '"(lines|statements|functions|branches)"' || echo "Coverage data not available"

      - name: Test Next.js build
        run: npm run build
        env:
          NODE_ENV: production
          NEXT_PUBLIC_API_URL: "http://localhost:5000"

      - name: Security scan with Semgrep
        if: always()
        continue-on-error: true
        run: |
          if [ -f .venv/bin/semgrep ]; then
            npm run semgrep:ci
          else
            echo "Semgrep not installed, skipping security scan"
          fi

  security-validation:
    name: Security Validation
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: cityforge_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate
        env:
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/cityforge_test"

      - name: Setup test database
        run: npx prisma db push
        env:
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/cityforge_test"

      - name: Validate log redaction functionality
        run: |
          echo "🔍 Testing log redaction with sample sensitive data..."
          npm run test:run -- src/lib/utils/log-redaction.test.ts --reporter=verbose
        env:
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/cityforge_test"

      - name: Check for hardcoded secrets
        run: |
          echo "🔍 Scanning for potential hardcoded secrets..."

          # More precise patterns to avoid false positives
          # Look for actual hardcoded secrets, not legitimate code patterns
          if grep -r -E "(secret|key|token|password)\s*[:=]\s*['\"][a-zA-Z0-9\-._~+\/]{16,}['\"]" src/ \
            --exclude-dir=node_modules \
            --exclude="*.test.*" \
            --exclude="*.spec.*" \
            --exclude-dir=__tests__ \
            --exclude="log-redaction.ts" \
            | grep -v "REDACTED" \
            | grep -v "process.env" \
            | grep -v "Enter" \
            | grep -v "Backspace" \
            | grep -v "key=" \
            | grep -v "key:" \
            | grep -v "className=" \
            | grep -v "pagination_default_limit"; then
            echo "❌ Potential hardcoded secrets found"
            exit 1
          fi

          # Check for actual database URLs with real credentials (not env vars)
          if grep -r -E "(postgres|mysql|mongodb)://[a-zA-Z0-9]+:[a-zA-Z0-9]+@" src/ \
            --exclude-dir=node_modules \
            --exclude="*.test.*" \
            --exclude="*.spec.*" \
            | grep -v "process.env" \
            | grep -v "REDACTED"; then
            echo "❌ Database URLs with credentials found"
            exit 1
          fi

          echo "✅ No hardcoded secrets detected"

  dependency-check:
    name: Dependency Security
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run npm audit
        run: |
          echo "🔍 Running npm security audit..."
          npm audit --audit-level=moderate

      - name: Check for known vulnerabilities
        continue-on-error: true
        run: |
          echo "🔍 Checking for known vulnerabilities..."
          npm audit --audit-level=high --dry-run

  status-check:
    name: CI Status
    runs-on: ubuntu-latest
    needs: [quality-checks, security-validation, dependency-check]
    if: always()

    steps:
      - name: Check job results
        run: |
          echo "Quality Checks: ${{ needs.quality-checks.result }}"
          echo "Security Validation: ${{ needs.security-validation.result }}"
          echo "Dependency Check: ${{ needs.dependency-check.result }}"

          if [[ "${{ needs.quality-checks.result }}" == "failure" || "${{ needs.security-validation.result }}" == "failure" ]]; then
            echo "❌ Critical checks failed"
            exit 1
          elif [[ "${{ needs.dependency-check.result }}" == "failure" ]]; then
            echo "⚠️  Dependency check failed, but allowing CI to continue"
            echo "Please review dependency vulnerabilities"
          else
            echo "✅ All checks passed"
          fi
