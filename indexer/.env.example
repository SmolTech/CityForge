# Indexer Environment Variables
# Copy this file to .env and configure for your environment

# OpenSearch Configuration
# Host where OpenSearch is running
OPENSEARCH_HOST=localhost
OPENSEARCH_PORT=9200

# Namespace for index isolation
# Use different namespaces for dev, staging, production
# Indices will be named: {NAMESPACE}_cards, {NAMESPACE}_cards_content
NAMESPACE=default

# Backend API URL
# Where to fetch card data from
# For local development: http://localhost:5000
# For Docker: http://backend:5000
# For production: https://api.yourdomain.com
BACKEND_URL=http://localhost:5000

# Optional: Indexing behavior
# Maximum number of pages to index per website
# MAX_PAGES_PER_SITE=10

# User agent for web crawling
# USER_AGENT=CityForge Indexer/1.0

# Crawl delay (seconds) - be respectful to websites
# CRAWL_DELAY=1.0
