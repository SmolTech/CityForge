apiVersion: v1
kind: Service
metadata:
  name: community-service
  namespace: community
  labels:
    app: cityforge
spec:
  selector:
    app: cityforge
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
