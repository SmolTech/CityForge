apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # Note: backend-secrets.yaml should be created from backend-secrets.yaml.template
  # - backend-secrets.yaml
  - postgres-config.yaml
  - deployment.yaml
  - service.yaml
  # backend-deployment.yaml - REMOVED (Flask backend no longer needed)
  - postgres.yaml
  - uploads-pvc.yaml
  - ingress.yaml
  - opensearch.yaml
  - indexer-cronjob.yaml
  - token-cleanup-cronjob.yaml

images:
  - name: ghcr.io/smoltech/cityforge/cityforge-frontend
    newTag: latest
  # ghcr.io/smoltech/cityforge-backend - REMOVED (Flask backend no longer needed)
  - name: ghcr.io/smoltech/cityforge/cityforge-indexer
    newTag: latest

namespace: community
