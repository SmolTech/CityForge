apiVersion: batch/v1
kind: CronJob
metadata:
  name: resource-indexer
  namespace: cityforge
  labels:
    app: resource-indexer
spec:
  schedule: "0 2 * * *" # Run daily at 2 AM
  jobTemplate:
    spec:
      template:
        spec:
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            runAsGroup: 1001
            fsGroup: 1001
          containers:
            - name: indexer
              image: ghcr.io/smoltech/cityforge-indexer
              imagePullPolicy: Always
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: false
                runAsNonRoot: true
                runAsUser: 1001
                runAsGroup: 1001
                capabilities:
                  drop:
                    - ALL
              env:
                - name: OPENSEARCH_HOST
                  value: "opensearch-service"
                - name: OPENSEARCH_PORT
                  value: "9200"
                - name: NAMESPACE
                  value: "cityforge"
                - name: POSTGRES_USER
                  valueFrom:
                    secretKeyRef:
                      key: username
                      name: cityforge.cityforge-db.credentials.postgresql.acid.zalan.do
                - name: POSTGRES_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      key: password
                      name: cityforge.cityforge-db.credentials.postgresql.acid.zalan.do
                - name: POSTGRES_HOST
                  valueFrom:
                    configMapKeyRef:
                      name: cityforge-config
                      key: POSTGRES_HOST
                - name: POSTGRES_PORT
                  valueFrom:
                    configMapKeyRef:
                      name: cityforge-config
                      key: POSTGRES_PORT
                - name: POSTGRES_DB
                  valueFrom:
                    configMapKeyRef:
                      name: cityforge-config
                      key: POSTGRES_DB
                # Updated to use Next.js API endpoint instead of Flask backend
                - name: BACKEND_URL
                  value: "http://cityforge-service/api"
              resources:
                requests:
                  memory: "128Mi"
                  cpu: "100m"
                limits:
                  memory: "512Mi"
                  cpu: "500m"
          restartPolicy: OnFailure
