# S3-compatible Object Storage Credentials
# This secret is required for using Linode Object Storage or other S3-compatible services
# 
# To create this secret:
# 1. Copy this file to s3-secret.yaml (DO NOT commit the actual secret)
# 2. Replace the placeholder values with your actual S3 credentials
# 3. Apply to your cluster: kubectl apply -f k8s/s3-secret.yaml
#
# For Linode Object Storage:
# - Get credentials from Linode Cloud Manager > Object Storage > Access Keys
# - Endpoint format: https://REGION.linodeobjects.com (e.g., https://us-east-1.linodeobjects.com)
# - Create a bucket first in the Linode Cloud Manager
#
# All values must be base64-encoded

apiVersion: v1
kind: Secret
metadata:
  name: cityforge-s3-credentials
  namespace: cityforge
type: Opaque
data:
  # Base64-encoded S3 endpoint (e.g., https://us-east-1.linodeobjects.com)
  # echo -n "https://us-east-1.linodeobjects.com" | base64
  s3_endpoint: aHR0cHM6Ly91cy1lYXN0LTEubGlub2Rlb2JqZWN0cy5jb20=
  
  # Base64-encoded bucket name
  # echo -n "your-bucket-name" | base64
  s3_bucket: eW91ci1idWNrZXQtbmFtZQ==
  
  # Base64-encoded S3 access key ID
  # echo -n "your-access-key-id" | base64
  s3_access_key_id: eW91ci1hY2Nlc3Mta2V5LWlk
  
  # Base64-encoded S3 secret access key
  # echo -n "your-secret-access-key" | base64
  s3_secret_access_key: eW91ci1zZWNyZXQtYWNjZXNzLWtleQ==
  
  # Base64-encoded region (optional, defaults to us-east-1)
  # echo -n "us-east-1" | base64
  s3_region: dXMtZWFzdC0x
