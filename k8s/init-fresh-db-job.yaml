apiVersion: batch/v1
kind: Job
metadata:
  name: init-fresh-db
  namespace: cityforge
  labels:
    app: init-fresh-db
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: init-fresh-db
    spec:
      restartPolicy: OnFailure
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: init-db
          image: ghcr.io/smoltech/cityforge-frontend
          imagePullPolicy: Always
          # Use initialization script that creates schema and seeds data
          command: ["/bin/sh"]
          args: ["/app/scripts/init-db-with-schema.sh"]
          env:
            # Database credentials from Postgres Operator secret
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  key: username
                  name: cityforge.cityforge-db.credentials.postgresql.acid.zalan.do
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: cityforge.cityforge-db.credentials.postgresql.acid.zalan.do
            # Connection metadata from ConfigMap
            - name: POSTGRES_HOST
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: POSTGRES_HOST
            - name: POSTGRES_PORT
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: POSTGRES_PORT
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: POSTGRES_DB
            # Optional: Set these to create an admin user automatically
            # - name: ADMIN_EMAIL
            #   value: "admin@example.com"
            # - name: ADMIN_PASSWORD
            #   valueFrom:
            #     secretKeyRef:
            #       key: admin-password
            #       name: cityforge-admin-secret
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            runAsNonRoot: true
            runAsUser: 1001
            runAsGroup: 1001
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
