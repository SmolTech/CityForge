apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cityforge-ingress
  namespace: cityforge
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"

spec:
  ingressClassName: public
  tls:
    - hosts:
        - cityforge.cityforge
        - www.cityforge.cityforge
      secretName: cityforge-tls
  rules:
    - host: "cityforge.cityforge"
      http:
        paths:
          - path: /api/config
            pathType: Prefix
            backend:
              service:
                name: cityforge-service
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: cityforge-backend-service
                port:
                  number: 5000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cityforge-service
                port:
                  number: 80
    - host: "www.cityforge.cityforge"
      http:
        paths:
          - path: /api/config
            pathType: Prefix
            backend:
              service:
                name: cityforge-service
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: cityforge-backend-service
                port:
                  number: 5000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cityforge-service
                port:
                  number: 80
